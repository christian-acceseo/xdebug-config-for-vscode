{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug 2",
            "type": "php",
            "request": "launch",
            "port": 9000,
            "pathMappings": {
                "/app": "${workspaceRoot}"
            },
            "xdebugSettings" : {
                "max_data": -1
            }
        }
    ]
}